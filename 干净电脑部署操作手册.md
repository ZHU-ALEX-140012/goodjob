# goodjob · 干净电脑从零部署操作手册（照做即可）

> 适用场景：一台什么都没装的 Windows 电脑（朋友机器），从零部署 goodjob 并跑起来。
> 每步都有「✅ 完成标志」，达到标志再进下一步。全程约 20~30 分钟（含下载等待）。
> 边做边用 `Win + G` 录屏，录完这份手册走一遍 = 视频素材齐了。

---

## 第 0 步｜出发前准备（在你自己电脑上做好，不需要 U 盘）

- [ ] 在你自己电脑上，把 `agent\goodjob-github` 整个文件夹**压缩成 zip**（右键 → 发送到 → 压缩(zipped)文件夹），改名 `goodjob.zip`。
  这一包里已经含：全部源码、`web_script.js`、`user_config.example.json`、操作手册要用的 `demo_config` 参照——到现场只需解它。
- [ ] 把 `goodjob.zip` 和这份手册发给自己的手机，任选其一：
  - **微信「文件传输助手」**（推荐，最简单）：电脑微信拖文件进去，到朋友电脑登微信（手机扫码）下载
  - QQ 发给自己 / 百度网盘上传后朋友电脑登录下载 / 邮箱发自己
  - 实在网也差：手机数据线连电脑，当 U 盘直接拷
- [ ] 手机装好微信（上面扫码登电脑版还要用它下 zip，一举两得）
- [ ] 到了朋友电脑先做隐私清理：退出/隐藏微信QQ钉钉、桌面清空个人文件、浏览器用**无痕窗口或新建配置档**

> ⚠️ 干净电脑没有 git，所以下载项目用 **Download ZIP** 或直接解微信/网盘带来的 `goodjob.zip`，不用 clone。

---

## 第 1 步｜安装 Python

1. 打开 Edge，访问 **python.org** → 右上角 `Downloads` → 点黄色按钮下载 Windows 安装包（64-bit）。
2. 双击安装包，**第一屏最下方务必勾选 `Add python.exe to PATH`** ★全场最容易翻车的一步，然后点 `Install Now`。
3. 装完按 `Win + R`，输入 `powershell` 回车，打开终端。

```powershell
python --version
```

- ✅ 完成标志：显示 `Python 3.12.x`（或更高）。
- ❌ 如果报「不是内部或外部命令」：第 2 条的 PATH 没勾。重新运行安装包 → Modify → 勾上 Add to PATH，或关掉所有终端窗口重开一个再试。

---

## 第 2 步｜下载并解压项目

1. Edge 打开 **https://github.com/ZHU-ALEX-140012/goodjob**
2. 点绿色 `Code` → `Download ZIP`，解压到桌面，得到文件夹 `goodjob-main`。
3. 把文件夹重命名为 `goodjob`（路径短，终端里好敲）。

>  不想现场从 GitHub 下：直接用第 0 步传到朋友电脑微信/网盘里的 `goodjob.zip` 解压，同样重命名为 `goodjob`，本步其余流程不变。

```powershell
cd $env:USERPROFILE\Desktop\goodjob
dir
```

- ✅ 完成标志：能看到 `main.py`、`web_script.js`、`user_config.example.json`、`requirements.txt` 这些文件。
- 💡 GitHub 打不开/太慢：用第 0 步传到微信/网盘的 `goodjob.zip` 直接解压。

---
前置条件：（检查系统编码）
### 🪟 Windows 系统

Windows 系统无法像 Linux 那样通过命令直接更改全局编码，但可以启用一个 **Beta 版功能**来将系统默认的 ANSI 代码页强制改为 UTF-8。

#### 如何检查
Windows 系统本身没有一个简单的命令来直观显示“系统编码”。你可以通过以下方式间接判断：
*   在命令提示符（CMD）或 PowerShell 中运行 `chcp` 命令。如果返回 `活动代码页: 65001`，则表示当前控制台会话使用的是 UTF-8 编码，但这并不代表系统全局编码已更改。
*   观察系统是否经常出现中文乱码，尤其是在使用一些较旧的软件时。

在 Windows 中，这通常指**仅对当前命令行窗口（会话）生效的修改，关闭窗口后即失效。根据你使用的终端类型，操作方法有所不同。

### 💻 命令提示符 (CMD)

在 CMD 窗口中，临时更改编码非常简单，只需执行一条命令：

```cmd
chcp 65001
```

执行后，当前窗口的代码页会立即切换为 UTF-8。你可以再次运行 `chcp` 命令来确认，如果返回“活动代码页: 65001”，则表示设置成功。

### 🚀 PowerShell

PowerShell 的临时修复稍微复杂一些，因为它涉及两个独立的编码变量。你需要按顺序执行以下三行命令，才能让中文显示和管道传输都恢复正常：

chcp 65001
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8

这三行命令的作用分别是：
1.  `chcp 65001`：将 Windows 控制台的代码页切换为 UTF-8。
2.  `[Console]::OutputEncoding = ...`：告诉 PowerShell 使用 UTF-8 来**显示**外部程序的输出。
3.  `$OutputEncoding = ...`：告诉 PowerShell 使用 UTF-8 来**编码通过管道传递**的数据。

执行完这三行命令后，你可以在当前窗口测试一下，例如运行 `echo "中文测试"`，应该能正常显示。但请注意，这些设置仅在**当前 PowerShell 窗口**有效，关闭后就会失效。

### 📝 通过环境变量临时设置

对于特定的开发场景（如 Python），你也可以在当前会话中临时设置环境变量来强制使用 UTF-8：

*   **在 CMD 中**：使用 `set` 命令。
    ```cmd
    set PYTHONIOENCODING=utf-8
    set PYTHONUTF8=1
    ```
    
*   **在 PowerShell 中**：使用 `$env:` 前缀。
    ```powershell
    $env:PYTHONUTF8=1
    ```
    
#### 如何永久更改为 UTF-8
1.  按下 `Win + R` 键，输入 `intl.cpl`，然后回车，打开“区域”设置窗口。
2.  切换到“**管理**”选项卡，点击“**更改系统区域设置...**”按钮。
3.  在弹出的窗口中，**勾选**“**Beta 版：使用 Unicode UTF-8 提供全球语言支持**”。
4.  点击“确定”，然后**重启电脑**使更改生效。

**⚠️ 重要提醒**：此设置是全局性的，可能会带来一些副作用，例如某些非常古老的软件可能出现乱码，或在使用 FAT32 格式的 U 盘时遇到问题。如果遇到兼容性问题，可以取消勾选并重启来恢复原设置。

## 第 3 步｜安装依赖

在项目目录的终端里：

```powershell
pip install -r requirements.txt
```

- 慢或失败就用国内镜像（视频评论区也要置顶这条）：

```powershell
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

- ✅ 完成标志：最后一行出现 `Successfully installed fastapi-... uvicorn-... pydantic-...`。
- 🎬 拍摄提示：这段下载画面后期 2~4 倍速快进。

---

## 第 4 步｜生成并修改配置 user_config.json

1. 在项目文件夹里，把 `user_config.example.json` **复制一份**，重命名为 `user_config.json`。
   （PowerShell 一行搞定：`Copy-Item user_config.example.json user_config.json`）
2. 用记事本（或 VSCode，视频里更好看）打开 `user_config.json`，最少改这两处：

```json
"tags": ["数据分析师", "数据分析", "BI 工程师"],
"introduce": "您好，我对贵司这个岗位很感兴趣，方便的话想请您看下我的简历。"
```

3. 其余字段（`scoring` 词表、`frontend.thread`、`match_count_threshold`、`auto_reply`、`notify`、`llm`）**演示时先不动**，口播带过即可。

- ✅ 完成标志：文件保存后，终端里执行下面命令不报 JSON 格式错误：

```powershell
python -c "import json;print(json.load(open('user_config.json',encoding='utf-8'))['tags'])"
```

显示你填的 tags 列表即通过。
- ❌ 报 `JSONDecodeError`：改配置时删多了逗号/引号，对照 `goodjob.zip` 里自带的 `user_config.example.json` 检查。

---

## 第 5 步｜启动本地后端

在项目目录的终端里：

```powershell
python main.py
```

- ✅ 完成标志，出现这两行：

```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Application startup complete.
```

- **这个终端窗口保持打开，全程别关。**
- 再开一个浏览器标签验证：访问 `http://127.0.0.1:8000/docs`，能看到接口列表（/tags、/get-job-score、/client-config…）说明后端活着。
- ❌ 报 `error while attempting to bind on address ('0.0.0.0', 8000)`：8000 被占用（朋友电脑一般不会有；真遇到就重启电脑再试一次）。

---

## 第 6 步｜部署油猴脚本（Tampermonkey）

1. Edge 地址栏输入 `edge://extensions/` → 左下角打开「允许来自其他商店的扩展」→ 点「获取 Microsoft Edge 扩展」→ 在扩展商店搜 **Tampermonkey** → 获取安装。
2. 安装后浏览器右上角拼图图标里固定 Tampermonkey 图标。
3. 点 Tampermonkey 图标 → `添加新脚本`（管理面板 → 追加新脚本）。
4. **全选删除编辑器里的模板内容**，粘贴项目里 `web_script.js` 的全文（就在解压出的 `goodjob` 文件夹里）→ `Ctrl + S` 保存。

- ✅ 完成标志：保存时不报语法错误，Tampermonkey 列表里出现该脚本且开关为「已启用」。

---

## 第 7 步｜打开 Boss 直聘跑起来

1. Edge 打开 **zhipin.com** 并登录（用备用小号最稳；正片里公司名/HR 名后期打码）。
2. 进入**职位搜索页**（搜任意岗位后的列表页）。
3. 什么都不用点，等几秒——页面左下角出现 goodjob 日志窗，开始滚动。

- ✅ 完成标志，日志窗依次出现类似：
  - `程序启动` / `获取标签成功`（这一步就是后端 `/tags` 通了）
  - 开始轮换关键词、打开详情页
  - `正在给职位 [xxx] 发送打招呼消息`、`匹配度: 100 命中匹配词: 5`
- ❌ 日志窗没出现：点 Tampermonkey 图标确认脚本已启用；确认当前是**职位列表/搜索页**而不是首页。
- ❌ 日志报「获取配置失败」：回到第 5 步，后端终端是不是被关了。
- 🎬 挂机跑 3~5 分钟，够剪开场钩子和镜头 6 的素材即可停。

---

## 第 8 步｜录完收尾（别忘了）

- [ ] 关掉 `python main.py` 终端（Ctrl+C 或直接关窗口）
- [ ] **删除朋友电脑上的 `user_config.json`**（若里面填过任何真实信息），以及整个 `goodjob` 文件夹可留可删
- [ ] Boss 直聘**退出登录**，关闭浏览器时清掉该站点登录态
- [ ] 检查录屏开头结尾没有拍到：微软账户邮箱、桌面个人文件、聊天弹窗

---

## 附：常见问题速查表

| 现象 | 原因 | 解决 |
|---|---|---|
| `python` 不是内部或外部命令 | 安装时没勾 Add to PATH | 重跑安装包 Modify 勾选，或重开终端 |
| pip 下载极慢/超时 | 默认走国外源 | 加 `-i https://pypi.tuna.tsinghua.edu.cn/simple` |
| 启动后端报端口占用 | 8000 被占 | 重启电脑再试；或 `python -m uvicorn main:app --port 8001`（前端配置 `frontend.serverHost` 也要同步改 8001） |
| Boss 页面无日志窗 | 脚本未启用/页面不对 | Tampermonkey 开关、确认是职位搜索列表页 |
| 日志「获取配置失败」 | 后端没在跑 | 重开 `python main.py`，窗口别关 |
| 一直不投简历只浏览 | 岗位命中不了词表/HR 不活跃 | 演示用属正常，口播解释 `match_count_threshold` 和活跃白名单即可 |
| JSON 保存报错 | 手改坏了逗号引号 | 对照 `user_config.demo.json` 恢复 |
